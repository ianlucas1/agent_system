[pytest]
# Measure coverage only on the core high-value modules currently under test.
addopts = --cov=src.tools.file_system --cov=src.handlers.command --cov=src.core.chat_session --cov=src.llm.clients --cov-report=term-missing --cov-fail-under=54 --doctest-glob="docs/*.md" 
markers =
    slow: marks tests as slow to run 